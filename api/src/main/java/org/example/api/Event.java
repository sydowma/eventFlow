package org.example.api;

public record Event(
        String id,
        String name,
        String description,
        String date
) {
    // No additional methods or fields are needed for this record class) {
}
