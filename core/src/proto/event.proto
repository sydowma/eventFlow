syntax = "proto3";

package com.example.event;

message Event {
  string id = 1;
  string type = 2;
  string data = 3;
  string timestamp = 4;
}

service LikeService {
  rpc LikeEvent(stream Event) returns (stream Event);
  rpc unlikeEvent(Event) returns (Event); // Keep unlikeEvent as unary
}